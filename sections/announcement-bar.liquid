<style>
  #announcement-bar {
    height: 38px;
    overflow: hidden;
    background-color: #1C2A3B;
    color: #fff;
    text-align: center;
    line-height: 38px;
  }

  #announcement-bar .swiper-slide > span {
    font-weight: bold;
    color: #FFDA12;
  }

  .swiper-slide-start {

  }
</style>

<div id="">
  <div id="announcement-bar">
    <div class="swiper-wrapper">
      <div class="swiper-slide">Easter Sale - <span>Save 300€ <span> on Carbon 1</div>
      <div class="swiper-slide">two</div>
      <div class="swiper-slide">three</div>
    </div>
  </div>
</div>

<script>
  var swiper = new Swiper('#announcement-bar', {
    direction: 'vertical',
    speed: 600,
    loop: true,
    autoplay: {
      disableOnInteraction: false,
      pauseOnMouseEnter: true,
      delay: 2000,
    },
    on: {
      init: function (swiper) {
        slide = this.slides.eq(0);
        slide.addClass('ani-slide');
      },
      transitionStart: function () {
        for (i = 0; i < this.slides.length; i++) {
          slide = this.slides.eq(i);
          slide.removeClass('ani-slide');
        }
      },
      transitionEnd: function () {
        slide = this.slides.eq(this.activeIndex);
        slide.addClass('ani-slide');
      },
    },
  });
</script>
